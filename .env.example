PORT=4000

# Supabase project settings (Project Settings -> API)
SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY   # server-only, never expose to the app

# Feature flag: keep false until a payment processor is integrated
REAL_MONEY_ENABLED=false
